make(chan Type)   //等价于make(chan Type, 0)
make(chan Type, capacity)

channel <- value  //发送vaule到channel
<-channel             //接收2并将其丢弃
x := <-channel     //从channel中接收数据，并赋值给x
x,ok := <-channel //功能同上，同时检查通道是否已关闭或者是否为空